# Buildless host-agent install for a Docker host managed by ComposeBastion.
# Copy to agent-compose.yml, set AGENT_TOKEN, then run:
#   docker compose -f agent-compose.yml up -d
#
# For production, pin COMPOSEBASTION_AGENT_VERSION to the same release as the
# manager, for example 1.0.6.
services:
  composebastion-agent:
    image: ${COMPOSEBASTION_AGENT_IMAGE:-ghcr.io/composebastion-admin/composebastion-agent}:${COMPOSEBASTION_AGENT_VERSION:-latest}
    pull_policy: always
    environment:
      AGENT_TOKEN: ${AGENT_TOKEN:?Set AGENT_TOKEN to a strong random token of at least 24 characters}
      AGENT_HOST: 0.0.0.0
      AGENT_PORT: 8090
    ports:
      - name: agent
        target: 8090
        published: "${COMPOSEBASTION_AGENT_PORT:-8090}"
        host_ip: ${COMPOSEBASTION_AGENT_BIND_ADDRESS:?Set COMPOSEBASTION_AGENT_BIND_ADDRESS to a trusted manager-reachable address}
        protocol: tcp
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8090/api/health',{headers:{authorization:'Bearer '+process.env.AGENT_TOKEN}}).then(async(r)=>process.exit(r.ok&&(await r.json()).ok===true?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped
